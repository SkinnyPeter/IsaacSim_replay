# Install these packages into the Python environment bundled with Isaac Sim.
# Isaac Sim itself supplies isaacsim, omni.*, pxr.*, and its rendering stack.

# Numerical and HDF5 data
numpy>=1.24,<2.0
scipy>=1.10,<2.0
h5py>=3.8,<4.0

# Images, plots, and MP4 output
opencv-python>=4.8,<5.0
Pillow>=10.0
imageio>=2.31,<3.0
imageio-ffmpeg>=0.4.9
matplotlib>=3.7,<4.0

# YAML configuration and local environment files
PyYAML>=6.0,<7.0
python-dotenv>=1.0,<2.0

# Optional dataset upload/download utilities
huggingface-hub>=0.24,<2.0
